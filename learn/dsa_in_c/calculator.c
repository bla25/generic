#include <stdio.h>
#include <math.h>
int x, y, sum , sub , mul, div, ch;
void sum1{
    sum = x+y;
    printf("the sum of the numbers is:%d", sum);
}

void sub1{
    sub = x-y;
    printf("the sub of the numbers is:%d", sub);
} 

void mul1{
    mul = x*y;
    printf("the multiplication of the numbers is:%d", mul);
}

void div1{
    div = x/y;
    printf("the multiplication of the numbers is:%d," div);
}

int main() {

}